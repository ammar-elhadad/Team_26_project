# Notes for Webviews and Cryptography modules
- Before testing webviews and crypto modules make sure the Backend server is running
- to run the backend server:
```bash
cd Flask-Backend
python -m venv venv
venv/Scripts/Activiate.ps1
pip install -r requirements.txt
python server.py
```
